<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.shwetasrivastava.twp">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:name="android.support.multidex.MultiDexApplication"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".LogIn">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".volunteer_page" />
        <activity android:name=".admin_page" />
        <activity android:name=".mark_attendance" />
        <activity android:name=".new_student" />
        <activity android:name=".new_volunteer" />
        <activity android:name=".view_progress" />
        <activity android:name=".upload_resources" />
        <activity android:name=".upload_schedule" />
        <activity android:name=".upload_progress" />
        <activity android:name=".download_resources" />
        <activity android:name=".view_attendance" />
        <activity android:name=".view_schedule" />
        <activity android:name=".student_select" />
        <activity android:name=".fill_report" />
        <activity android:name=".fill_report2" />
        <activity android:name=".volun_select" />
        <activity android:name=".upload_volun_progress" />
        <activity android:name=".volun_select2"></activity>
    </application>

</manifest>